# aws-lambda-r - settings

- Settings / Secrets for .sh scripts (and R too)
- By default, to avoid sharing secrets, new files are ignored by git
- In .gitignore include only files to be shared, such as example files
